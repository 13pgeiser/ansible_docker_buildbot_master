galaxy_info:
  role_name: ansible_docker_buildbot_master
  author: Pascal Geiser
  description: Install my buildbot master docker image.
  license: license MIT
  min_ansible_version: 2.8
  galaxy_tags:
    - debian
    - buster
    - docker
    - buildbot
  dependencies:
    - { name: 'ansible_docker', src: 'git+https://13pgeiser@github.com/13pgeiser/ansible_docker.git' }

